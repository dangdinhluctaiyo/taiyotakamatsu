// Database Service - Using Supabase
// To switch back to Cloudflare API, change the import below

// Option 1: Use Supabase (recommended)
export { db } from './supabase';

// Option 2: Use Cloudflare D1 API (comment out above, uncomment below)
// export { db } from './db-cloudflare';
